.blog-container
  %h1 Editing Blog Post
  = render 'form'
  -# = link_to 'Show', blog_post_path(@blog_post.id, @blog_post.slug), class: 'btn btn-default'
  -# |
  -# = link_to 'Back', blog_posts_path, class: 'btn btn-default'
  = link_to 'Show', @blog_post, class: 'btn btn-default'
  |
  = link_to 'Back', blog_posts_path, class: 'btn btn-default'
